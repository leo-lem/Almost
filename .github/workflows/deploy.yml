name: Deploy static content to Pages
on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths: ["resource/web/**"]
permissions:
  pages: write
  id-token: write
  contents: read
jobs:
  deploy:
    uses: leo-lem/leo-lem/.github/workflows/deploy-pages.yml@main
    with:
      path: web
